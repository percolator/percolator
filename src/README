This package produces two different executables Luminator and Qvalumity.

Please read INSTALL file (located in parent directory) in order
to build the package.

The commands `./qvalumity --help' or  `./luminator --help' in created 'bin'
folder will give full usage statements.
