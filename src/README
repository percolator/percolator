This package produces two different executables Qvality and Percolator.

Please read INSTALL file (located in parent directory) in order
to build the package.

The commands `./qvality --help' or  `./percolator --help' in created 'bin'
folder will give full usage statements.
